#include "main.h"
#include <stdio.h>
/**
* _abs - Computes the absolute value of an integer.
*
* Description: _abs comptues value for integer.
* @i: input number as an integer.
*
*Return: Absolute value
*/
int _abs(int i)
{
if (i >= 0)
{
return (1);
}
else
{
return (i * -1);
}
}
