#include "main.h"
/**
*positive_or_negative function - Test function for positive or negative.
*Description: void positive_or_negative function for i.
*@i: determinet i is positve or negative or zero.
*Return: 0
*/
void positive_or_negative(int i);
{
if (i > 0)
printf("%d is possitive\n", i);
else if (i < 0)
printf("%d is negative\n", i);
else
printf("%d is zero\n", i);
}
